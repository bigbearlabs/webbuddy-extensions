### Get started

```
# command-line deps
gem install slim
npm install -g brunch
gem install -g grunt

# project deps
npm install

# go
grunt server --watch
open _public  # load this dir as an unpacked extension in chrome.
```
